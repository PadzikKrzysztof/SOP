/*
Deployment script for SOP_DataBase

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "SOP_DataBase"
:setvar DefaultFilePrefix "SOP_DataBase"
:setvar DefaultDataPath "C:\Users\padzi\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\padzi\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Availability].[DayTimeeEnd] is being dropped, data loss could occur.

The column [dbo].[Availability].[DayTimeStart] is being dropped, data loss could occur.

The column [dbo].[Availability].[DayID] on table [dbo].[Availability] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Availability].[TimeEnd] on table [dbo].[Availability] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.

The column [dbo].[Availability].[TimeStart] on table [dbo].[Availability] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Availability])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The column [dbo].[Employee].[Email] is being dropped, data loss could occur.

The column [dbo].[Employee].[LoginProfileID] is being dropped, data loss could occur.

The column [dbo].[Employee].[PhoneNumber] is being dropped, data loss could occur.

The column [dbo].[Employee].[PaymentInfoID] on table [dbo].[Employee] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Employee])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The column [dbo].[Patient].[Email] is being dropped, data loss could occur.

The column [dbo].[Patient].[LoginProfileID] is being dropped, data loss could occur.

The column [dbo].[Patient].[PhoneNumber] is being dropped, data loss could occur.

The type for column PESEL in table [dbo].[Patient] is currently  BIGINT NOT NULL but is being changed to  INT NOT NULL. Data loss could occur and deployment may fail if the column contains data that is incompatible with type  INT NOT NULL.
*/

IF EXISTS (select top 1 1 from [dbo].[Patient])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Dropping Foreign Key [dbo].[FK_Availability_Doctor]...';


GO
ALTER TABLE [dbo].[Availability] DROP CONSTRAINT [FK_Availability_Doctor];


GO
PRINT N'Dropping Foreign Key [dbo].[FK_Employee_LoginProfile]...';


GO
ALTER TABLE [dbo].[Employee] DROP CONSTRAINT [FK_Employee_LoginProfile];


GO
PRINT N'Dropping Foreign Key [dbo].[FK_Patient_LoginProfile]...';


GO
ALTER TABLE [dbo].[Patient] DROP CONSTRAINT [FK_Patient_LoginProfile];


GO
PRINT N'Starting rebuilding table [dbo].[Availability]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Availability] (
    [Id]        INT      IDENTITY (1, 1) NOT NULL,
    [TimeStart] TIME (7) NOT NULL,
    [TimeEnd]   TIME (7) NOT NULL,
    [DoctorID]  INT      NOT NULL,
    [DayID]     INT      NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Availability])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Availability] ON;
        INSERT INTO [dbo].[tmp_ms_xx_Availability] ([Id], [DoctorID])
        SELECT   [Id],
                 [DoctorID]
        FROM     [dbo].[Availability]
        ORDER BY [Id] ASC;
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_Availability] OFF;
    END

DROP TABLE [dbo].[Availability];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Availability]', N'Availability';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Altering Table [dbo].[Employee]...';


GO
ALTER TABLE [dbo].[Employee] DROP COLUMN [Email], COLUMN [LoginProfileID], COLUMN [PhoneNumber];


GO
ALTER TABLE [dbo].[Employee]
    ADD [PaymentInfoID] INT NOT NULL;


GO
PRINT N'Altering Table [dbo].[Patient]...';


GO
ALTER TABLE [dbo].[Patient] DROP COLUMN [Email], COLUMN [LoginProfileID], COLUMN [PhoneNumber];


GO
ALTER TABLE [dbo].[Patient] ALTER COLUMN [PESEL] INT NOT NULL;


GO
PRINT N'Creating Table [dbo].[BankInfo]...';


GO
CREATE TABLE [dbo].[BankInfo] (
    [Id]          INT          IDENTITY (1, 1) NOT NULL,
    [BankDetails] VARCHAR (50) NULL,
    [Name]        VARCHAR (50) NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);


GO
PRINT N'Creating Table [dbo].[Day]...';


GO
CREATE TABLE [dbo].[Day] (
    [Id]         INT IDENTITY (1, 1) NOT NULL,
    [DayOfMonth] INT NOT NULL,
    [Month]      INT NOT NULL,
    [Year]       INT NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);


GO
PRINT N'Creating Table [dbo].[FileNote]...';


GO
CREATE TABLE [dbo].[FileNote] (
    [Id]          INT           IDENTITY (1, 1) NOT NULL,
    [Name]        VARCHAR (50)  NULL,
    [Description] VARCHAR (MAX) NULL,
    [LabTestID]   INT           NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);


GO
PRINT N'Creating Table [dbo].[PaymentInfo]...';


GO
CREATE TABLE [dbo].[PaymentInfo] (
    [Id]         INT          IDENTITY (1, 1) NOT NULL,
    [Addres]     VARCHAR (50) NOT NULL,
    [BankNumber] INT          NOT NULL,
    [BankInfoID] INT          NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);


GO
PRINT N'Creating Foreign Key [dbo].[FK_Availability_Doctor]...';


GO
ALTER TABLE [dbo].[Availability] WITH NOCHECK
    ADD CONSTRAINT [FK_Availability_Doctor] FOREIGN KEY ([DoctorID]) REFERENCES [dbo].[Doctor] ([Id]);


GO
PRINT N'Creating Foreign Key [dbo].[FK_Availability_Day]...';


GO
ALTER TABLE [dbo].[Availability] WITH NOCHECK
    ADD CONSTRAINT [FK_Availability_Day] FOREIGN KEY ([DayID]) REFERENCES [dbo].[Day] ([Id]);


GO
PRINT N'Creating Foreign Key [dbo].[FK_Day_MonthEnum]...';


GO
ALTER TABLE [dbo].[Day] WITH NOCHECK
    ADD CONSTRAINT [FK_Day_MonthEnum] FOREIGN KEY ([Month]) REFERENCES [dbo].[MonthEnum] ([Id]);


GO
PRINT N'Creating Foreign Key [dbo].[FK_FileNote_LabTest]...';


GO
ALTER TABLE [dbo].[FileNote] WITH NOCHECK
    ADD CONSTRAINT [FK_FileNote_LabTest] FOREIGN KEY ([LabTestID]) REFERENCES [dbo].[LabTest] ([Id]);


GO
PRINT N'Creating Foreign Key [dbo].[FK_PaymentInfo_BankInfo]...';


GO
ALTER TABLE [dbo].[PaymentInfo] WITH NOCHECK
    ADD CONSTRAINT [FK_PaymentInfo_BankInfo] FOREIGN KEY ([BankInfoID]) REFERENCES [dbo].[BankInfo] ([Id]);


GO
PRINT N'Creating Foreign Key [dbo].[FK_Employee_PaymentInfo]...';


GO
ALTER TABLE [dbo].[Employee] WITH NOCHECK
    ADD CONSTRAINT [FK_Employee_PaymentInfo] FOREIGN KEY ([PaymentInfoID]) REFERENCES [dbo].[PaymentInfo] ([Id]);


GO
PRINT N'Checking existing data against newly created constraints';


GO
USE [$(DatabaseName)];


GO
ALTER TABLE [dbo].[Availability] WITH CHECK CHECK CONSTRAINT [FK_Availability_Doctor];

ALTER TABLE [dbo].[Availability] WITH CHECK CHECK CONSTRAINT [FK_Availability_Day];

ALTER TABLE [dbo].[Day] WITH CHECK CHECK CONSTRAINT [FK_Day_MonthEnum];

ALTER TABLE [dbo].[FileNote] WITH CHECK CHECK CONSTRAINT [FK_FileNote_LabTest];

ALTER TABLE [dbo].[PaymentInfo] WITH CHECK CHECK CONSTRAINT [FK_PaymentInfo_BankInfo];

ALTER TABLE [dbo].[Employee] WITH CHECK CHECK CONSTRAINT [FK_Employee_PaymentInfo];


GO
PRINT N'Update complete.';


GO
